## What changed

Fixes #

## Test plan

- [ ] `./gradlew assembleDebug` and `./gradlew testDebugUnitTest` pass
- [ ] `./gradlew jacocoCoverageVerification` passes (if pure logic changed)
- [ ] Tested on a real device, screenshot attached (if UI/behavior visible on screen changed)
